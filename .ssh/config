host github.com
 HostName github.com
 # If you don't want to pass the identitiy file as an env var, you need to hardcode the path here
 # IdentityFile ~/projects/lala/.ssh/id_rsa
 User git
 # You may want to change the proxy ip to use another exit node
 ProxyCommand nc -X 5 -x 10.64.0.1:1080 %h %p
